import s from "../../../styles/main.module.scss";

const UserData = () => {
  return (
    <>
      <div className={s.adds}>
        <section className={s.myAdds}></section>

        <section className={s.banner}></section>
      </div>
    </>
  );
};

export default UserData;
